package com.contextenglish.entity.enums;

/** Determines how a single AcceptableAnswer is matched against learner input. */
public enum AnswerType {
    EXACT,
    SYNONYM,
    KEYWORD,
    CATEGORY
}
